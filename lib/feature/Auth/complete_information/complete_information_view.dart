import 'package:flutter/material.dart';
import 'package:fruitsmarket/feature/Auth/complete_information/widgets/complete_information_body.dart';

class completeinformation_view extends StatelessWidget {
  const completeinformation_view({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body:completeinformationbody() ,
    );
  }
}
